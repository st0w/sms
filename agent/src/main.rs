// Placeholder — the agent is implemented in M3 (scan, catalog sync, Tailscale
// HTTPS ranged streaming, hybrid-token verification, tag writeback).
fn main() {
    println!("music-agent placeholder; implemented in M3");
}
