# shared

Cross-component contracts. Populated in **M2**: the OpenAPI spec for the
control-plane API and the capability-token payload schema (hybrid Ed25519 +
ML-DSA, `alg`/key-id header, JWKS-style key distribution).
